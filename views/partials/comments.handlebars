<div class="comments">
    {{#each this}}
    <div class="comment">
        <p>{{user.username}} on {{format_date created at}}</p>
    </div>
    <div class="text">
        {{comment_description}}
    </div>
    {{/each}}
</div>